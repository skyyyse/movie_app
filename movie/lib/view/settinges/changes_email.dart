import 'package:flutter/material.dart';
import 'package:get/get_utils/get_utils.dart';

class changes_email extends StatelessWidget {
  const changes_email({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      appBar: AppBar(
        title:  Text("Changes Email".tr),
        elevation: 0,
        centerTitle: true,
      ),
    );
  }
}
